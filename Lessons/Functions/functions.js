// function createTag(name, prefix, sufix) {
//     if(prefix && sufix){
//         return `${prefix} ${name} ${sufix}`;
//     }if (prefix) {
//         return `${prefix} ${name}`;
//     }
//     return name;
// }

// const tag = createTag("Nelson","[Programmer]","Dev");
// console.log(tag);

// function pow(number, exponent) {
//     return number ** exponent;
// }

// console.log(pow(2,4));


export function sum(a,b){
    return a + b;
}